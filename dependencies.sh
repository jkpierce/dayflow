sudo apt update
sudo apt install zenity
sudo apt install \
    taskwarrior \
    timewarrior \
    jq \
    libnotify-bin \
    pulseaudio-utils \
    tmux \
    python3-pip


mkdir -p ~/dayflow/lib
mkdir -p ~/dayflow/sounds

cd ~/dayflow
